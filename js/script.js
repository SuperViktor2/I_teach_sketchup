function about () {
	var element = document.querySelector("#about");
	element.scrollIntoView({ behavior: 'smooth', block: 'start'});
}

function courses () {
	var element = document.querySelector("#program");
	element.scrollIntoView({ behavior: 'smooth', block: 'start'});
}

function apps () {
	var element = document.querySelector("#apps");
	element.scrollIntoView({ behavior: 'smooth', block: 'start'});
}
